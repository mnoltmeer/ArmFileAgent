[+] перенести керування з'єднаннями та потоками у спеціальний клас

[+] розробити функціонал обміну даними з сервером конфігурацій

[+] переробити взаємодію з ELI, замість постійного підключення ліби підключати
    її тільки коли надходить скрипт та для виконання стартового скрипту

[+] розробити алгоритм оновлення програми

[+] програма має звіряти перелік файлів у каталозі даних з переліком, що надійшов від серверу
    відсутні у ньому файли мають бути видалені з каталогу даних

[+] сервер конфігів постійно згортає дерево адресної книги
    дерево адр. книги перебудовується тільки якщо додається новий клієнт

[+] сервер конфігів перевіряє статус групи public

[+] доробити та перевірити завантаження конфігів у потоці
    потрібно правильно юзати CoInitialize()/CoUninitialize()
    можливо перенести весь функціонал GetConfigurationFromServer() у поток
    перенесено GetConfigurationFromServer() у поток

[+] агент постійно повторно скачує та ініціалізує конфіги з'єднань

[+] зробити так, щоб після старту запускались не тільки оновлені з'єднання, але й наявні

[+] врахувати ситуацію, коли втрачено зв'язок з сервером конфігів
    агент має стартанути з'єднання у штатному режимі

[+] при виникненні виключення під час формування переліку конфігів для клієнта,
    сервер не відправляє дані
    причина у відправці порожніх даних сервером, коли стається виключення у формуванні переліку файлів
    це штатна поведінка

[+] агент встигає ініціалізувати з'єднання з локального файлу, коли сервер надсилає файл
    це пов'язано з тим, що виникає виключення під час перевірки версії локального файлу

[?] прикрутити шифрування до обміну даними

[+] агент має автоматично запускати виконавчі файли, що надійшли (.exe, .bat, .cmd)

[+] агент має зупиняти запущені ним модулі, якщо надійшло оновлення цих файлів з серверу

[+] агент перед видаленням зайвих файлів має зупиняти модулі

[+] агент автоматично запускає модуль ArmAgent.exe_ тому що він потрапляє у вибірку файлів
    за маскою *.exe. Треба переробити зміну імені оновленого файлу модуля.

[+] не перейменовувати файл модуля Програми при завантаженні, щоб мати змогу контролювати версійність
    і не перекачувати його при кожному сеансу обміну.

[+] змінити алгоритм CheckAndRunExistModules(), щоб він не запускав автоматично
    основний модуль Програми з каталогу даних

[х] допрацювати алгоритм ProcessLoadedFile() так, щоб коли опрацьовується модуль Програми
    була перевірка по стандартній схемі: версія-дата-розмір і тільки тоді запускалось оновлення

[+] перенести звірку версій наявного модулю та оновлення у Гардіан

[+] привести до ладу інтерфейс сервера конфігів
    - додати більше інфи в перелік файлів
    - зробити експорт адресної книги разом з прив'язкою до переліку файлів

[x] додати у сервер конфігів можливість редагувати скрипти та конфіги
    - скрипти на базі ELI IDE
    - конфіги на базі FFExCfgCrt

[+] розробити оновлену консоль адміністрування на базі AMRA

[+] перевірити процес оновлення Гардіана

[+] реалізувати імпорт адресної книги с сервера конфігів

[+] домогтись, щоб вікна створення групи та редагування запису з'являлись в центрі
    основної форми сервера конфігів

[+] тести
    + коректність мігрованих даних з Менеджеру
    + запуск під всіма користувачами
    + зупинка і запуск при зміні користувача
    + оновлення Агента
    + оновлення Гардіана

[x] доробити UI Сервера конфігів, дані щодо файлів у переліку конфігів мають оновлюватись
    для правильного відображення
    вони і так оновлюються при виборі запису в адресній книзі

[+] зробити в Консолі кнопку "Запит до серверу" примусово запустити оновлення з сервера конфігів

[+] після завантаження файлу з сервера конфігів, йому має бути прописана дата змін
    така ж як у файлі на сервері

[+] якщо в адресну книгу сервера конфігів додається вже існуючий хост, треба не
    додавати запис, а змінювати властивості існуючого

[+] редагування запису в адресній книзі сервера конфігів не працює, не дає змінити дані хосту
